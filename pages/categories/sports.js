import Head from "next/head";
import Layout from "../../components/layout";
import NewsCard from "../../components/newsCard";
import styles from "../../styles/Home.module.css";

export const getStaticProps = async () => {
  const res = await fetch(
    "https://newsapi.org/v2/top-headlines?country=in&category=sports&apiKey=5d82c7ac29bd43d58fffd2b6a2909c9e"
  );
  const data = await res.json();

  // render 404 if no data fetched
  if(data.artcles.length === 0) {
    return {
      notFound: true
    };
  }

  return {
    props: { headlines: data.articles },
    revalidate: 3600
  };
};

const Sports = ({ headlines }) => {
  headlines.map((article) => (article.source.id = Math.random()));
  return (
    <div>
      <Head>
        <title>Sports</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Layout />
      <main className={styles.main}>
        <h1>Sports</h1>
        {headlines.map((article) => (
          <NewsCard
            key={article.source.id}
            urlToImage={article.urlToImage}
            title={article.title}
            description={article.description}
            author={article.author}
            url={article.url}
          />
        ))}
      </main>
    </div>
  );
}
 
export default Sports;